<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="mx.com.audioweb.divisas_project" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".CrearAlerta"
            android:label="@string/app_name"
            android:theme="@style/CustomActionBarThemeAlert"
            >
        </activity>
        <activity
            android:name=".Login"
            android:label="@string/app_name"
            android:theme="@android:style/Theme.Holo.Light.NoActionBar.Fullscreen"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MenuOption"
            android:label="@string/title_activity_menu"
            android:theme="@style/CustomActionBarThemeAlert"
            >
        </activity>
        <activity
            android:name=".MisAlertas"
            android:label="@string/title_activity_my_divisas"
            android:theme="@style/CustomActionBarTheme">
        </activity>
    </application>

</manifest>
